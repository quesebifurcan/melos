(ns melos.scores.graph
  (:require [schema.core :as s]
            [plumbing.graph :as graph]
            [plumbing.core :as plumbing]))

(def score-graph [])

(def section-graph [])

(defn compose-parameters [])

(defn changes [])
